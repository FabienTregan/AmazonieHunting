var config = {

    // if set to 'true', will hide the mouse pointer (usefull on touchscreens)
    hideMouse: true,

    // time in seconds before the screensaver appears. If 0, screensaver is disabled.
    screensaverDelay : 120,
}